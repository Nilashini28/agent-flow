/**
 * EscalationPanel — Stage 5+ placeholder.
 * Approve/reject UI requires backend Stage 5 (not yet built).
 * This stub renders nothing and compiles cleanly.
 */
export default function EscalationPanel(_props: { runId: string }) {
  // Deferred to Stage 5 — human-approval flow depends on backend escalation endpoints.
  return null;
}
