/**
 * Timeline — Stage 5+ standalone panel (placeholder).
 * RunViewer already renders the timeline inline; this component
 * is reserved for the multi-panel dashboard layout in a later stage.
 */
export default function Timeline(_props: { runId: string }) {
  return null;
}
